# Database connection settings
hibernate.connection.driver_class=com.mysql.cj.jdbc.Driver
hibernate.connection.url=jdbc:mysql://localhost:3306/wigell_camping_members_club?serverTimezone=UTC
hibernate.connection.username=root
hibernate.connection.password=3732

# SQL Dialect
hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Show SQL in console (helpful for debugging)
hibernate.show_sql=true
hibernate.format_sql=true
hibernate.highlight_sql=true

# Manage schema (create-drop for testing, update for persistence)
hibernate.hbm2ddl.auto=none